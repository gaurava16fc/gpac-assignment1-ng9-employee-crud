import { StrictNumericDigtsDirective } from './strict-numeric-digts.directive';

describe('StrictNumericDigtsDirective', () => {
  it('should create an instance', () => {
    const directive = new StrictNumericDigtsDirective();
    expect(directive).toBeTruthy();
  });
});
