import { StrictNumericDecimalDigtsDirective } from './strict-numeric-decimal-digts.directive';

describe('StrictNumericDecimalDigtsDirective', () => {
  it('should create an instance', () => {
    const directive = new StrictNumericDecimalDigtsDirective();
    expect(directive).toBeTruthy();
  });
});
